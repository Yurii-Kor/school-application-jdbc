# HikariCP Configuration for H2 (in-memory test database)
jdbcUrl=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
username=sa
password=
maximumPoolSize=10
minimumIdle=2
idleTimeout=60000
connectionTimeout=30000
autoCommit=true